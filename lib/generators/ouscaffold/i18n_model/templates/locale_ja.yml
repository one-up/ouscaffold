ja:
  activerecord:
    models:
      "<%= file_path %>": "<%= translate(singular_name) %>" #g

    attributes:
      "<%= file_path %>":
<%- attributes.each do |attr| -%>
        <%= attr.name %>: "<%= translate(attr.name) %>" #g
<%- end -%>
